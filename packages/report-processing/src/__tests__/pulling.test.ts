// import { run } from '../pulling'
//
// import { waitMessages } from '../services/queue'
// import { processMessage } from '../services/report-processing'
//
// jest.mock('../services/queue', () => {
//   waitMessages: jest.fn()
// })
// jest.mock('../services/report-processing', () => {
//   processMessage: jest.fn()
// })
//
describe('report-processing', () => {
  // it('Check initial calls for entry point', (done) => {
  //   setTimeout(() => {
  //     expect(waitMessages).toBeCalledTimes(1)
  //     expect(processMessage).toBeCalledTimes(1)
  //     done()
  //   }, 0)
  //   run()
  // })
  it('to be added', () => expect(true).toBe(true))
})
